# Copy this file to .env.local and fill in real values. .env.local is gitignored.

# Base URL of the Symfony API backend. Server-side only (no NEXT_PUBLIC_
# prefix) - pages fetch it from Server Components, the browser never talks
# to this origin directly, so it never needs to ship to the client bundle.
API_URL=http://localhost:8000
